version: '3.8'

services:
  subwoofer:
    build: .
    container_name: subwoofer_bot
    restart: unless-stopped
    volumes:
      # Mapowanie pliku .env by można było łatwo zmienić token na produkcji bez przebudowy
      - ./.env:/app/.env
      # Logi również wyciągamy na zewnątrz by były łatwo dostępne
      - ./bot.log:/app/bot.log
